/**
 * Google Maps — API key del proyecto
 * 1. https://console.cloud.google.com/google/maps-apis
 * 2. Crear key → habilitar "Maps JavaScript API"
 * 3. Pegar la key abajo (restricción HTTP: tu dominio Netlify)
 */
const MAPS_CONFIG = {
  apiKey: '',
};

---
description: Contexto del proyecto Comunidad Azul (sitio, Stitch, Supabase, convenciones)
alwaysApply: true
---

# Comunidad Azul — contexto del proyecto

**Ruta:** `C:\Users\Santiago Montenegro\comunidad-azul`  
**Sitio:** https://comunidad-azul.netlify.app  
**Repo:** https://github.com/SantiagoMontenegroC/comunidad-azul (único; no usar `comunidad-azul-59f8c`)  
**Memoria extendida:** leer `AGENTS.md` en la raíz del repo.

**Tipo:** Sitio estático (Barrio Divino Niño 1, Colombia). Ex-prototipo WordPress; ahora HTML/CSS/JS + Supabase. Deploy Netlify en push a `main` (ver `despliegue-automatico.mdc`).

## Diseño (Google Stitch)

- Referencia visual Stitch; tipografías **Raleway** + **Nunito**; iconos **Material Symbols Outlined**.
- Imágenes: `img/stitch/` (`hero-agua.jpg`, `mapa-suministro.jpg`, etc.).
- **Sin olas SVG** en hero ni footer (versión final acordada).

## Estructura `index.html`

1. Navbar fija  
2. `#inicio` — hero + `.hero-info` (feature-cards)  
3. `#puntos-venta` — tarjetas + mapa Leaflet (`js/puntos-venta.js`)  
4. `#reportes` — formulario + feed Realtime (`js/reportes.js`)  
5. `#contacto` — formulario (`js/contacto.js`)  
6. Footer  

Stats del hero desde Supabase (`js/main.js`). WhatsApp: **+57 320 641 2955** (`wa.me/573206412955`).

## Supabase

- URL: `https://xyzyqkxdvdmyoevetxyw.supabase.co` — `js/supabase.js`
- Schema: `supabase/schema.sql` — tablas `puntos_venta`, `reportes`, `contactos`
- Realtime en `reportes`; puntos con `lat`/`lng` para mapa

## Mapa

Leaflet + OpenStreetMap (sin API de pago). Coordenadas en `AGENTS.md`.

## Fuera del repo

- Capturas: `Documents\Comunidad-Azul-capturas\`
- Word Fase 3: OneDrive `...\Proyecto de grado\Comunidad Azul_fase 3(Santiago Montenegro).docx`
- No subir: `docs/`, `.cursor/`, scripts auxiliares (`.gitignore`)

## Preferencias

- Español; commits en español; sin `Co-authored-by: Cursor`
- No Python para parches de sitio; editar archivos directamente
- No añadir: `proyecto-aviso`, `footer__academico`, `dato-demo`, “referencia educativa”

## Git

Santiago Montenegro / `santiagom17calderon@gmail.com` — GitHub **SantiagoMontenegroC** — `gh` en `%LOCALAPPDATA%\Programs\gh-cli\bin\gh.exe`
